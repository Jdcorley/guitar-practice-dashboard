export component Metronome inherits Rectangle {
    background: #1D2630;
    HorizontalLayout {
        padding: 16px; spacing: 12px;
        Text { color: #E6E6E6; text: "Metronome (placeholder)"; }
        // Requires std-widgets; will add later if needed
    }
}


